# =============================================================================
# gh (GitHub CLI) config
# =============================================================================

version: "1"
git_protocol: ssh
editor: code --wait
prompt: enabled
pager: ""

aliases:
  co: pr checkout
  pv: pr view
  pc: pr create
  rv: repo view --web
